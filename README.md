# Voice Recorder Web Application

Веб-приложение для записи и распознавания речи на Django с использованием OpenAI Whisper, Docker, PostgreSQL, Celery и Redis.

## Возможности

- 🎤 Загрузка аудио файлов
- 🔍 Распознавание речи с помощью Whisper (в фоне через Celery)
- 📊 Личный кабинет с дашбордом
- 💾 Хранение записей в PostgreSQL
- 📥 Скачивание аудио и текста
- ⚙️ Выбор модели Whisper (качество/скорость)
- 🎨 Современный темный интерфейс
- 🐳 Docker контейнеры для простого развертывания
- ⚡ Redis кеширование
- 🔄 Асинхронная обработка через Celery

## Быстрый старт

### Локальная разработка

1. **Клонируйте репозиторий и перейдите в директорию:**

```bash
# cd web (не нужно)
```

2. **Скопируйте файл с переменными окружения:**

```bash
cp .env.example .env
```

Отредактируйте `.env` при необходимости.

3. **Запустите с помощью Docker Compose:**

```bash
docker-compose up --build
```

Приложение будет доступно по адресу: http://localhost:8000/

4. **Примените миграции (если нужно):**

```bash
docker-compose exec web python manage.py migrate
docker-compose exec web python manage.py createsuperuser
```

### Продакшен

1. **Скопируйте файл переменных для продакшена:**

```bash
cp .env.prod.example .env
```

Отредактируйте `.env` с реальными значениями (особенно SECRET_KEY, пароли БД).

2. **Запустите в продакшен режиме:**

```bash
docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
```

3. **Настройте SSL сертификаты** (раскомментируйте HTTPS секцию в `nginx/conf.d/default.conf`)

## Структура проекта

```

├── voice_recorder/      # Настройки Django проекта
├── recordings/          # Основное приложение
├── templates/           # HTML шаблоны
├── static/              # Статические файлы
├── nginx/               # Nginx конфигурация
├── Dockerfile           # Docker образ для продакшена
├── Dockerfile.dev       # Docker образ для разработки
├── docker-compose.yml   # Основной compose файл
├── docker-compose.override.yml  # Переопределения для разработки
├── docker-compose.prod.yml      # Переопределения для продакшена
├── entrypoint.sh        # Скрипт инициализации
└── requirements.txt     # Зависимости Python
```

## Сервисы

- **web** - Django приложение (Gunicorn в продакшене, runserver в разработке)
- **db** - PostgreSQL база данных
- **redis** - Redis для кеша и Celery
- **celery** - Worker для фоновых задач
- **celery-beat** - Планировщик задач (опционально)
- **nginx** - Веб-сервер (только в продакшене)

## Команды Docker Compose

### Разработка

```bash
# Запуск
docker-compose up

# Запуск в фоне
docker-compose up -d

# Остановка
docker-compose down

# Просмотр логов
docker-compose logs -f

# Выполнение команд
docker-compose exec web python manage.py migrate
docker-compose exec web python manage.py createsuperuser
docker-compose exec web python manage.py shell

# Перезапуск сервиса
docker-compose restart web
```

### Продакшен

```bash
# Запуск
docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d

# Остановка
docker-compose -f docker-compose.yml -f docker-compose.prod.yml down

# Обновление
docker-compose -f docker-compose.yml -f docker-compose.prod.yml pull
docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
```

## Переменные окружения

См. `.env.example` для списка всех переменных окружения.

Основные:
- `SECRET_KEY` - секретный ключ Django (обязательно изменить!)
- `DEBUG` - режим отладки (False для продакшена)
- `POSTGRES_*` - настройки базы данных
- `CELERY_BROKER_URL` - URL для Celery broker
- `ALLOWED_HOSTS` - разрешенные хосты

## Миграции базы данных

```bash
# Создать миграции
docker-compose exec web python manage.py makemigrations

# Применить миграции
docker-compose exec web python manage.py migrate
```

## Статические файлы

Статические файлы автоматически собираются при запуске контейнера через entrypoint.sh.

Для ручной сборки:
```bash
docker-compose exec web python manage.py collectstatic --noinput
```

## Мониторинг Celery

```bash
# Просмотр логов Celery worker
docker-compose logs -f celery

# Проверка статуса задач
docker-compose exec web celery -A voice_recorder inspect active

# Мониторинг в реальном времени
docker-compose exec web celery -A voice_recorder events
```

## Резервное копирование

### База данных

```bash
# Создать бэкап
docker-compose exec db pg_dump -U postgres voice_recorder > backup.sql

# Восстановить
docker-compose exec -T db psql -U postgres voice_recorder < backup.sql
```

### Медиа файлы

Медиа файлы хранятся в `./media/`, просто скопируйте эту директорию.

## Производительность

### Оптимизации

- PostgreSQL с connection pooling
- Redis кеширование
- Gunicorn с 4 workers
- Celery для асинхронной обработки
- Nginx для статических файлов
- Gzip сжатие

### Масштабирование

Для увеличения производительности:

1. Увеличьте количество Celery workers:
```yaml
celery:
  command: celery -A voice_recorder worker --concurrency=4
```

2. Увеличьте количество Gunicorn workers:
```yaml
web:
  command: gunicorn --workers 8 ...
```

3. Настройте ресурсы в `docker-compose.prod.yml`

## Безопасность

1. Обязательно измените `SECRET_KEY` в `.env`
2. Используйте сильные пароли для БД
3. Включите HTTPS в продакшене
4. Настройте `ALLOWED_HOSTS`
5. Отключите `DEBUG` в продакшене

## Решение проблем

### Проблемы с подключением к БД

Убедитесь что PostgreSQL контейнер запущен:
```bash
docker-compose ps
docker-compose logs db
```

### Проблемы с Celery

Проверьте логи:
```bash
docker-compose logs celery
```

Убедитесь что Redis запущен:
```bash
docker-compose logs redis
```

### Очистка

```bash
# Остановить и удалить контейнеры и volumes
docker-compose down -v

# Очистить неиспользуемые образы
docker system prune -a
```

## Лицензия

Открытый исходный код.
